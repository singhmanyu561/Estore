import React from 'react';
import './SearchBar.css';

const SearchBar = () => {
    return(
        <div className="SearchBar">
            SearchBar
        </div>
    );
}

export default SearchBar;